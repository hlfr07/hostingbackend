<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Ejemplo Simple</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; }
    .message { color: blue; margin-bottom: 20px; }
    .user { padding: 10px; border-bottom: 1px solid #ccc; }
  </style>
</head>
<body>
  <h1>Página Principal</h1>
  
  <!-- Mensaje del servicio -->
  <div class="message">
    <h2><%= message.text %></h2>
  </div>

  <!-- Datos del servicio -->
  <div class="data">
    <h3>Lista de Usuarios:</h3>
    <% message.data.forEach(user => { %>
      <div class="user">
        ID: <%= user.id %> - Nombre: <%= user.name %>
      </div>
    <% }); %>
  </div>
</body>
</html>